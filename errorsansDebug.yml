---
- hosts: ubuntu
  become: yes
  tasks:
  - name: install git
    command: hostname -f
    #tags
    register: gitresult
    when: ansible_os_family =="Debian"
  - name: display the result of git
    debug:
      msg: "{{gitresult}}"
  
  